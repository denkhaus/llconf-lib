(copy template
	(and
		(rsync present)
	    (or 
			(test "diff" [join [join [var:work_dir] "/templates/"] [arg:0]] [arg:1])
	        (change "rsync" [join [join [var:work_dir] "/templates/"] [arg:0]] [arg:1])
		)
	)
)


(rsync present
	(installed "rsync")
)